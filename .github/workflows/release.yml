name: Needle 2 Android Release

on:
  workflow_dispatch:
  push:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Runner test
        run: echo "GitHub Actions runner is working"
